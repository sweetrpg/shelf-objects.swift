name: PR Validation

on:
  pull_request:
    branches:
      - master
      - develop

permissions:
  contents: write

jobs:
  validate:
    uses: sweetrpg/github-actions/.github/workflows/swift-ci.yaml@master
    with:
      swift-version: "6.3.3"
      publish-coverage: false
    secrets: inherit
